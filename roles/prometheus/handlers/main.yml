---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart prometheus
  ansible.builtin.service:
    name: prometheus
    state: restarted

- name: Reload prometheus
  ansible.builtin.service:
    name: prometheus
    state: reloaded

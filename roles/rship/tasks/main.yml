---
# rship — Layer 3: rship service (worker nodes)
# TODO: Implement when provisioning rship LXC containers

- name: TODO
  ansible.builtin.debug:
    msg: "rship role applied to {{ inventory_hostname }} — tasks pending implementation"
